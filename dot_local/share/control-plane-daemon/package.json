{
  "name": "control-plane-daemon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mattermost-connected control-plane daemon (KAN-2). Listens for DMs from the operator and replies; real commands land in follow-up tickets.",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tsx": "^4.23.12",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "msw": "^2.15.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
